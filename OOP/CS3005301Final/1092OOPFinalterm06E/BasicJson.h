#pragma once
#include <map>

using namespace std;

struct mystruct {


};
class BasicJson {
public:
	vector<map<string,>>
	bool Parse(std::string InputFileName);
	void Write(std::string OutputFileName);
	void Delete(std::string Name);
	float TotalArea();	
	vector<map> operator[]
};